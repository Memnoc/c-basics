#include <stdio.h>

int main() {

  // constant = fixed value that cannot be altered by the program during its
  // execution

  const float PI = 31.14159; // capital as convention

  // pi = 420.69; // this would give an error

  printf("%f\n", PI);
  return 0;
}
